# onChange

This project focuses on React form handling and controlled inputs.

Key files:
- [src/README.md](src/README.md) explains the event handlers and state patterns.

What it demonstrates:
- `onChange` handlers
- controlled inputs
- form state updates with spread syntax
- simple toggle interactions

Run it with:
```bash
npm install
npm run dev
```
