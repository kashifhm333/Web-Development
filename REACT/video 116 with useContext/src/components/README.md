# video 116 with useContext/src/components

This folder contains the nested components that read from Context.

Key files:
- [Button.jsx](Button.jsx) wraps the deeper component tree.
- [Component1.jsx](Component1.jsx) reads the context value.
- [Navbar.jsx](Navbar.jsx) composes the UI.

What it demonstrates:
- shared state through context
- nested component composition
