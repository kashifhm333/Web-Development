# video 116 with useContext

This project demonstrates the Context API as a replacement for prop drilling.

Key files:
- [src/README.md](src/README.md) explains the provider/consumer flow.

What it demonstrates:
- `createContext`
- `Context.Provider`
- `useContext`
- shared state without prop chains

Run it with:
```bash
npm install
npm run dev
```
