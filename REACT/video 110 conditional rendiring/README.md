# video 110 conditional rendering

This project demonstrates conditional rendering patterns in React.

Key files:
- [src/README.md](src/README.md) explains ternaries, `&&`, and list rendering.

What it demonstrates:
- showing and hiding UI
- mapping arrays to JSX
- state-driven view changes

Run it with:
```bash
npm install
npm run dev
```
