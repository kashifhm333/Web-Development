# useRef

This project demonstrates `useRef` for DOM access and mutable values that do not trigger re-renders.

Key files:
- [src/README.md](src/README.md) explains the ref usage and patterns.

What it demonstrates:
- direct DOM access
- `.current` references
- imperative updates with `useEffect`

Run it with:
```bash
npm install
npm run dev
```
