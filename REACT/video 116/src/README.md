# video 116/src

This folder contains the components used in the `video 116` prop-drilling example.

Key files:
- [components/README.md](components/README.md) explains `Button.jsx` and `Component.jsx`.

What it demonstrates:
- component nesting
- passing values through child components
- presentational components that only display props
