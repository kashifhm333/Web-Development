# component props and JSX

This project shows how to pass data with props and build reusable JSX components.

Key files:
- [src/README.md](src/README.md) explains the Card component and App usage.

What it demonstrates:
- props destructuring
- reusable presentational components
- rendering the same component with different data

Run it with:
```bash
npm install
npm run dev
```
