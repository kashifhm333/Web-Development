# video 115

This project demonstrates React Router navigation in a Vite app.

Key files:
- [src/README.md](src/README.md) explains the routing setup and link components.

What it demonstrates:
- `BrowserRouter`
- `Routes` and `Route`
- `Link` navigation

Run it with:
```bash
npm install
npm run dev
```
