# react useEffect

This project demonstrates how `useEffect` works with mount, update, and cleanup patterns.

Key files:
- [src/README.md](src/README.md) explains the dependency-array examples in detail.

What it demonstrates:
- side effects
- cleanup functions
- dependency arrays
- prop-driven effects in child components

Run it with:
```bash
npm install
npm run dev
```
